name: aptos_balance_debug
description: Debug app — private key to APT balance
publish_to: 'none'
version: 1.1.3+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  convert: ^3.1.1
  pointycastle: ^3.9.1
  flutter_secure_storage: ^9.2.2
  qr_flutter: ^4.1.0
  share_plus: ^8.0.0
  intl: ^0.19.0
  url_launcher: ^6.2.5
  mobile_scanner: ^5.2.3
  shared_preferences: ^2.5.5
  file_selector: ^0.9.3
  http_parser: ^4.0.2
  flutter_image_compress: ^2.3.0
  dio: ^5.4.0         
  open_filex: ^4.4.0  
  path_provider: ^2.1.2
 
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/GTA.gif 
    - assets/apt.png
    - assets/mee.png
    - assets/mega.png   
    - assets/3.gif
    - assets/usdt.png
    - assets/usdc.png
